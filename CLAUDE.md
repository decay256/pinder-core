# Claude Code Instructions

Use `AGENTS.md` in this repository as the source of truth for project rules, commands, architecture notes, and safety constraints.

Before editing code, read `AGENTS.md`, inspect the relevant files, and preserve uncommitted user changes.

When finishing, run the validation commands listed in `AGENTS.md`, summarize changed files, and call out anything that could not be verified locally.

