# Contract: Issue #492 — LlmPlayerAgent

## Component: `Pinder.SessionRunner`

### 1. `PlayerAgentContext`
Add properties needed for character immersion:
- `IReadOnlyList<ConversationEntry>? History { get; }`
- `string TextingStyleFragment { get; }`
- `ActiveArchetypeInfo? ActiveArchetype { get; }`

### 2. `Program.cs`
- Pass `session.History`, `sable.TextingStyleFragment`, and `sable.ActiveArchetype` into `PlayerAgentContext` when instantiated.

### 3. `LlmPlayerAgent`
- Update `BuildPrompt()` to include conversation history, texting style, and active archetype behavior.
- Use `claude-sonnet-4-20250514`.
- Send request using `AnthropicClient`.
- Parse the choice using `PICK: [A-D]`.
- Return `PlayerDecision` with the LLM's reasoning and the `OptionScore[]` array generated by `ScoringPlayerAgent` fallback.
