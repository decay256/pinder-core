Suppressed by exception: Unsafe Leakage of GameApi Raw Exception/HTML Content (resp.text) to public-facing HTTP responses - would be U1.

### Finding 1: Media Upload and Fetch Proxy Has No Regression Coverage
**File**: `pinder-web/src/Pinder.GameApi/Controllers/MediaController.cs:39`
**Issue**: `MediaController` exposes `POST /api/v1/media/upload` and `GET /api/v1/media/{id}` with multipart upload assembly, Bearer-token forwarding, upstream status mapping, and streamed asset fetch behavior, but the test suite has no matching tests: `rg "MediaController|UploadPhoto|GetPhoto|api/v1/media|photobooth|PINDER_ASSET_STORE|AssetStoreUrl|AssetStoreToken" src/Pinder.GameApi.Tests src/pinder-backend/tests frontend/src frontend/e2e` returns no test hits. The endpoint branches at lines 48, 62, 87, 94, 118, 126, 137, and 140 are therefore unpinned.
**Impact**: A change can break user photo upload/download, drop the asset-store authorization header, return the wrong public URL, or regress upstream failure handling without any unit, integration, or contract test failing.
**Urgency**: U2 - topic default because this is a public API/external persistence boundary with meaningful state and error behavior but no direct regression coverage.
**Fixer-Agent Action Plan**: Add `Pinder.GameApi.Tests/Controllers/MediaControllerTests.cs` using `WebApplicationFactory<Program>` plus a fake `IHttpClientFactory`/message handler. Cover empty upload 400, missing `AssetStoreUrl` 503, successful multipart POST metadata/payload/Authorization/header/content-type/public URL, upstream non-2xx mapping, invalid UUID 400, missing asset 404, successful GET stream content type, and shared-secret 401. Run `dotnet test src/Pinder.GameApi.Tests/Pinder.GameApi.Tests.csproj`.

### Finding 2: Placement-State Mutation Is Only Tested Below the API Boundary
**File**: `pinder-web/src/pinder-backend/routes/sessions.py:106`
**Issue**: The owner-only public mutation `PUT /api/sessions/{session_id}/placement-state` calls `proxy.update_placement_state(...)`, which then forwards to GameApi at `session_services.py:450`; GameApi handles the write at `src/Pinder.GameApi/Controllers/SessionsController.Creation.cs:119`. Current tests cover serializer/model behavior and a frontend helper URL (`frontend/src/api/deleteSession.test.ts:132`), but `rg "placement-state|update_session_placement_state|UpdatePlacementStateAsync|/api/sessions/.*/placement-state|/sessions/.*/placement-state" src/Pinder.GameApi.Tests src/pinder-backend/tests frontend/src frontend/e2e` finds no FastAPI route test and no C# controller test for the PUT boundary.
**Impact**: The path that actually enforces ownership, forwards the payload, preserves validation errors, maps 404/400/502, and returns the echoed `placement_state` can regress while repository serialization and frontend URL tests still pass.
**Urgency**: U2 - topic default because this is a user-owned state transition and persistence/API contract boundary.
**Fixer-Agent Action Plan**: Add FastAPI tests that override `require_auth` and `SessionProxyService` to assert correct owner-checked forwarding, 401/403/404 propagation, and 400 validation response shape for `/api/sessions/{id}/placement-state`. Add GameApi controller tests for `UpdatePlacementStateAsync`: missing body field 400, unknown session 404, valid placement 200 with snake_case `placement_state`, invalid placement 400, and missing `X-GameApi-Key` 401. Run the relevant pytest session tests and `dotnet test src/Pinder.GameApi.Tests/Pinder.GameApi.Tests.csproj`.
