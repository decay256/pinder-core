> Scope: LLM-dirt changed-code gate for #1341; only the user-specified changed files were eligible for findings.

### Finding 1: Emotional director operation is still test-only and never reaches the DATEE reply path
**File**: `src/Pinder.LlmAdapters/PinderLlmAdapter.EmotionalDirector.cs:17`
**Issue**: `GenerateEmotionalDirectionAsync(DateeContext)` builds and sends the new private director request, including `LlmPhase.EmotionalDirector` at `src/Pinder.LlmAdapters/PinderLlmAdapter.EmotionalDirector.cs:56` and `src/Pinder.LlmAdapters/PinderLlmAdapter.EmotionalDirector.cs:82`, but the active DATEE production path in `src/Pinder.LlmAdapters/PinderLlmAdapter.cs:217` still builds `SessionDocumentBuilder.BuildDateePrompt(...)` and sends exactly one `LlmPhase.OpponentResponse` call at `src/Pinder.LlmAdapters/PinderLlmAdapter.cs:234`. The focused regression currently locks that unwired behavior: `CurrentDateeResponsePath_RemainsSingleOpponentResponseCallWithNoDirectorCall` asserts a single `OpponentResponse` phase and no director content at `tests/Pinder.LlmAdapters.Tests/Issue1341_EmotionalDirectorContractTests.cs:260`.
**Impact**: The #1341 director contract, prompt entry, parser, retry handling, diagnostics, temperature default, and `LlmPhase.EmotionalDirector` production phase can only run when tests call the internal method directly. Real gameplay DATEE replies continue without the private emotional direction step, so the new runtime surface can drift or break without affecting production until a later wiring pass.
**Urgency**: U3 - topic default; this is explicit dead/unwired changed code, but the changelog and docs state the current DATEE path intentionally does not invoke the director, so it is maintainability debt rather than an immediate correctness regression.
**Fixer-Agent Action Plan**: Decide whether #1341 was meant to stop at a private operation or wire into DATEE orchestration. If it should be live, call `GenerateEmotionalDirectionAsync(context, cancellationToken)` from the stateful `GetDateeResponseAsync` flow before building/sending the visible DATEE reply, pass the resulting private direction into the DATEE prompt through a non-visible/contracted field or prompt section, and replace the current no-director characterization test with an integration test that observes the ordered `emotional_director` then `opponent_response` phases. If the operation is intentionally deferred, track a follow-up ticket and keep the code marked as staged to prevent accidental deletion or silent contract drift.

Inspected: `CHANGELOG.md`, `Directory.Build.props`, `agent.log` relevant #1340/#1341 entries, `data/prompts/emotional-reactions.yaml`, `docs/data-architecture.md`, `docs/generated-json-output.md`, `docs/modules/llm-adapters.md`, `docs/prompts.md`, `src/Pinder.Core/Interfaces/LlmPhase.cs`, `src/Pinder.LlmAdapters/EmotionalReactionPromptCatalog.cs`, `src/Pinder.LlmAdapters/LlmPhaseTemperatures.cs`, `src/Pinder.LlmAdapters/PinderLlmAdapter.cs`, `src/Pinder.LlmAdapters/EmotionalDirectorContract.cs`, `src/Pinder.LlmAdapters/PinderLlmAdapter.EmotionalDirector.cs`, and `tests/Pinder.LlmAdapters.Tests/Issue1341_EmotionalDirectorContractTests.cs`. Prior reports checked for dedupe: `doc-code-mismatches.md`, `dry-violations.md`.
