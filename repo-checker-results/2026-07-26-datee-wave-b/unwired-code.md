> Scope: current #1338/#1339 sprint working tree (29 modified/untracked Core files, excluding generated audit reports) plus `pinder-web/src/Pinder.GameApi.Tests/Services/CharacterSynthesisServiceTests.cs` (30 files total).

### Finding 1: Emotional-reaction catalog is mandatory but has no live consumer
**File**: `src/Pinder.LlmAdapters/EmotionalReactionPromptCatalog.cs:80`
**Issue**: The new runtime accessors `GetInterestStateMeaning(...)`, `GetRelationshipTransitionInstruction(...)`, and `GetEventMeaning(...)` are called only by `Issue1339_EmotionalReactionPromptCatalogTests`; the sole production reference to this class is `PromptCatalog.ValidateRuntimeCatalog()` calling `EmotionalReactionPromptCatalog.ValidateRuntimeCatalog(this)`. The GameApi ships and loads `emotional-reactions.yaml`, and the generic admin prompt service lists/saves it, but `DateeResponseStage` and `SessionDocumentBuilder.BuildDateePromptEx` still build the live DATEE response from the older interest narrative and failure-guidance keys without retrieving any of the 71 new keys.
**Impact**: Editing any new emotional-reaction prompt cannot change a DATEE response, even though a missing or malformed entry can now reject production startup/reload. The catalog is therefore operationally required data with no gameplay effect until the downstream #1340 director path consumes it.
**Urgency**: U3 - topic default; `agent.log` explicitly identifies #1340 as the downstream consumer for these prerequisite contracts.
**Fixer-Agent Action Plan**: In #1340, derive the typed interest meaning, relationship transition, and stat/outcome event key from the resolved turn, retrieve them through these accessors, substitute the transition placeholders, and inject the resulting direction into the operation-local DATEE director request. Add a production-path test showing that changing one catalog entry changes the director prompt/trace, not merely catalog lookup tests.

### Finding 2: Nine required diagnosis formulations are stored but never forwarded to DATEE behavior
**File**: `src/Pinder.Core/Characters/TherapistDiagnosisContract.cs:17`
**Issue**: `safe_connection`, `hurt_protection`, `repair_requirement`, and the six `*_reaction` fields are now mandatory, generated, present in all six modified bundled characters, and preserved by the generic dictionary loader/writer and web DTO/editor paths. In production behavior, however, `TurnOrchestrator.BuildCognitiveSubtext` still reads only `DerivedFeelingKey` and `DefenseReactionKey`; no runtime source outside validation/generation reads any of the nine new constants or values.
**Impact**: Character synthesis spends the expanded 900-token diagnosis budget and session setup rejects characters missing these values, but changing the values cannot affect DATEE output. The character-specific half of the planned emotional direction remains inert until #1340 forwards it.
**Urgency**: U3 - topic default; the fields are deliberately staged prerequisites for the explicitly downstream #1340 runtime path.
**Fixer-Agent Action Plan**: Extend the #1340 director input/context with the canonical diagnosis map (or a typed projection), select the relevant stat reaction from the actual resolved stat, and include the safe-connection, hurt-protection, and repair requirements where the director contract specifies. Add an end-to-end prompt-capture test proving that two characters with different formulation values produce different director input while loader/writer and admin round trips retain every field.
