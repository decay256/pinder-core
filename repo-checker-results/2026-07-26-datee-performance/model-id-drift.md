> Scope: files changed in 075cadd..HEAD for the DATEE emotional-performance sprint (22 files; repo-checker output excluded). Topic: model-id-drift. Prior reports reviewed and not duplicated.

### Finding 1: EmotionalDirector diagnostics escape the canonical operation-kind vocabulary
**File**: `src/Pinder.LlmAdapters/PinderLlmAdapter.cs:1332`
**Issue**: `MapOperationKind` maps `LlmPhase.OpponentResponse` to `OperationalDiagnosticOperationKind.DateeResponse`, but it has no branch for the newly routed `LlmPhase.EmotionalDirector`; unrecognized phases fall through via `return phase ?? LlmPhase.Unknown;`. The new DATEE path now emits transport diagnostics for `EmotionalDirector` and `OpponentResponse`, so the director call is reported with raw operation kind `"emotional_director"` while the visible performance call reports canonical operation kind `"datee_response"`.
**Impact**: Model/provider/phase routing identifiers are otherwise centralized (`LlmPhase.EmotionalDirector`, YAML-owned director temperature/max_tokens, and `LlmPhase.OpponentResponse` for the visible DATEE response), but observability consumers grouping by `OperationalDiagnosticOperationKind` will see the private director under an ad hoc raw phase rather than the established DATEE-response operation family or an explicit new operation-kind constant. That can split DATEE latency/error/cost dashboards and make EmotionalDirector versus OpponentResponse behavior harder to compare during #1344/#1345.
**Urgency**: U3 - topic default; this is classification/observability drift, not a runtime routing bug or hardcoded model slug.
**Fixer-Agent Action Plan**: Decide whether `LlmPhase.EmotionalDirector` should map to `OperationalDiagnosticOperationKind.DateeResponse` or to a new canonical `OperationalDiagnosticOperationKind.EmotionalDirector`; update `MapOperationKind` and the emotional-director exhaustion diagnostic to use the same operation-kind policy, then add a focused regression that asserts director transport diagnostics and terminal contract-exhaustion diagnostics use the chosen canonical operation kind while retaining `phaseCode == LlmPhase.EmotionalDirector`.
