> Scope: changed-code audit topic 5/11 (`trivial-tests`) over the 22 explicitly listed current changed files in `A:\Data\ClaudeCodex\pinder-web\pinder-core`.

### Finding 1: GameDefinition guard tests assert only absence of exceptions
**File**: `tests/Pinder.LlmAdapters.Tests/Issue1217_ExplicitGameDefinitionTests.cs:114`
**Issue**: `Production_GetDialogueOptions_DoesNotThrow_WhenGameDefinitionIsProvided` and `Production_GetDateeResponse_DoesNotThrow_WhenGameDefinitionIsProvided` both use `var exception = await Record.ExceptionAsync(...); Assert.Null(exception);` without asserting the returned dialogue options, DATEE response text, transport phases, or emotional-director handoff. In the current sprint these tests were updated to feed an `EmotionalTurnEvent` and director response, but they still pass as long as the adapter does not throw.
**Impact**: These tests can stay green while the "provided GameDefinition" production path returns the wrong dialogue option payload, skips the director/performance phase sequence, or otherwise produces unusable gameplay output. That weakens the regression signal exactly around the adapter boundary this sprint is changing.
**Urgency**: U2 — topic default; the tests are regression-adjacent and cover a production adapter boundary, but adjacent suites already exercise most returned-contract behavior so this is ticket-worthy rather than a sprint-gate blocker.
**Fixer-Agent Action Plan**: Replace the no-exception pattern with result assertions. For dialogue options, await `GetDialogueOptionsAsync`, assert the expected option count/stat/text, and optionally assert the fake transport observed the dialogue phase. For DATEE response, await `GetDateeResponseAsync`, assert the expected response text and that the fake transport saw `EmotionalDirector` before `OpponentResponse`.
