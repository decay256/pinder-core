> Scope: pinder-core 7962415f750de354b53d4f9b953eaa3e37b3575b..ae95e83cf40909337ccb0e9651b86bacae24972d; pinder-web c7a465bb67fb86ee6b5ab1105955b7c0717eddca..271b2230. Current HEAD only; findings limited to changed-scope endpoints/files.

### Finding 1: Character update service exposes an unused wrapper bypassed by the instrumented endpoint
**File**: `pinder-web/src/Pinder.GameApi/Services/CharacterDefinitionUpdateService.cs:46`
**Issue**: `public async Task<CharacterDetail> UpdateAsync(...)` only chains `BuildValidatedDefinitionAsync(...)` and `SaveValidatedDefinitionAsync(...)`, but no production or test code calls it (`rg "UpdateAsync\("` only finds the declaration; unrelated matches are EF `ExecuteUpdateAsync`). The changed `CharactersController.PutCharacter` endpoint wires the service by calling `.BuildValidatedDefinitionAsync(slug, payload, cancellationToken)` and `.SaveValidatedDefinitionAsync(slug, definition, cancellationToken)` separately so it can emit `ValidateInput` and `PersistArtifact` operation phases around the save.
**Impact**: The unused public method is dead surface on the admin character-update path. If a future caller discovers it and uses it as the "obvious" service API, it will perform the mutation without the operation-tracker phase instrumentation that the changed endpoint intentionally preserves.
**Urgency**: U3 - topic default; this is maintainability/dead surface, not currently reachable production behavior.
**Fixer-Agent Action Plan**: Delete `UpdateAsync` and keep the two currently wired methods as the service contract, or, if a single-call API is desired, redesign it to accept instrumentation hooks/operation context and update `CharactersController.PutCharacter` to use that single path without losing phase emission.
