> Scope: full pinder-core and pinder-web repositories at the finalized post-fix main commits.

### Finding 1: Single-text LLM response normalization is duplicated outside the overlay helper
**File**: `pinder-core/src/Pinder.LlmAdapters/PinderLlmAdapter.cs:694`
**Issue**: `GetSuccessImprovementAsync` trims `responseText`, treats blank or `"..."` as `empty_output`, emits `RaiseOverlayDegraded(...)`, strips surrounding quotes, and returns the normalized text at lines 694-720. `GetSteeringQuestionAsync` repeats the same trim/blank/degradation/quote-strip pattern at lines 759-773, and `GetHorninessQuestionAsync` repeats it again at lines 807-821 with only the terminal behavior changed from `return string.Empty` to `throw new InvalidOperationException(...)`. `GetInterestChangeBeatAsync` has the same quote-strip branch at lines 364-379. This sits next to the already-centralized `NormalizeOverlayRewriteResult(...)` helper at line 991, so the remaining single-text methods are bypassing an established normalization pattern.
**Impact**: Production LLM response cleanup can drift between success-improvement, steering, horniness-question, and interest-beat paths. A future change to quote stripping, empty-output telemetry, ellipsis handling, or refusal/meta-control rejection has to be patched method-by-method.
**Urgency**: U3 — topic default.
**Fixer-Agent Action Plan**: Extract a small helper for single-text LLM outputs that performs trim, optional ellipsis rejection, optional quote stripping, degradation event creation, and caller-specific terminal behavior. Rewire success improvement, steering question, horniness question, and interest-change beat through it, then run `Pinder.LlmAdapters.Tests` plus the core turn-delivery tests that exercise steering/horniness/success-improvement paths.

### Finding 2: Operational diagnostic terminal emission is copied across conversation stages
**File**: `pinder-core/src/Pinder.Core/Conversation/DateeResponseStage.cs:159`
**Issue**: `DateeResponseStage.ExecuteAsync` has paired `catch (OperationCanceledException ex)` and `catch (Exception ex)` blocks at lines 159-201 that construct terminal `OperationalDiagnosticEvent` objects, set cancelled/failed outcomes, classify exceptions, attach `callId`, and add the same `["turn"] = state.TurnNumber...` hint. The same terminal-emission shape is duplicated in `DeliveryStage.ExecuteAsync` at `pinder-core/src/Pinder.Core/Conversation/DeliveryStage.cs:496` and in `TurnOrchestrator.LoadDialogueOptionsAsync` at `pinder-core/src/Pinder.Core/Conversation/TurnOrchestrator.cs:280`, differing mostly by source/event names, operation kind, and phase code.
**Impact**: Diagnostic lifecycle behavior can drift across the three production turn stages. Changes to cancellation severity, failure classification, correlation hints, or terminal event naming require coordinated edits in multiple hot-path catch blocks.
**Urgency**: U3 — topic default.
**Fixer-Agent Action Plan**: Add helper methods to `OperationalDiagnostics`, for example `EmitCancelledTerminal(...)` and `EmitFailedTerminal(...)`, that take source/event/message/operation/phase/callId/turn. Replace the three copied catch-block event constructions and run the diagnostic lifecycle tests for `DateeResponseStage`, `DeliveryStage`, and dialogue-option loading.

### Finding 3: Core tests duplicate ILlmAdapter no-op boilerplate despite shared stubs
**File**: `pinder-core/tests/Pinder.Core.Tests/ComboGameSessionTests.cs:19`
**Issue**: `ComboTestLlmAdapter` locally implements `ILlmAdapter` and repeats pass-through overlay methods such as `ApplyHorninessOverlayAsync(...) => Task.FromResult(message)`, `ApplyShadowCorruptionAsync(...) => Task.FromResult(message)`, `ApplyTrapOverlayAsync(...) => Task.FromResult(message)`, and `ApplyFailureCorruptionAsync(...)` at lines 50-56. The same no-op adapter surface appears broadly in tests such as `Conversation/Issue293_MarkEndedTests.cs:19`, `Issue1168_DcBiasTests.cs:26`, `TrapTaintInjectionTests.cs:20`, and `WeaknessWindowTests.cs:409`; a repository-wide check found 49 local `ILlmAdapter`/`IStatefulLlmAdapter` test classes and 127 repeated pass-through overlay method implementations. `pinder-core/tests/Pinder.Core.TestCommon/StubLlmAdapter.cs:14` and production `pinder-core/src/Pinder.Core/Conversation/NullLlmAdapter.cs:14` already centralize default no-op adapter behavior.
**Impact**: Every `ILlmAdapter` signature change or default overlay behavior change forces many unrelated test edits. It also encourages local fakes to drift from shared test defaults while still exercising the same engine paths.
**Urgency**: U3 — topic default.
**Fixer-Agent Action Plan**: Extend `StubLlmAdapter` with the missing hooks these local fakes need, such as queued stateful responses, context capture, and per-method delegates. Replace local no-op implementations with `StubLlmAdapter` subclasses or configured instances that override only behavior under test, then run `Pinder.Core.Tests`.

### Finding 4: A core test file reimplements repository/data path discovery already in TestRepoLocator
**File**: `pinder-core/tests/Pinder.Core.Tests/Issue840_SingleLoaderPipelineTests.cs:55`
**Issue**: `Issue840_SingleLoaderPipelineTests` defines `LoadJson(...)` at lines 55-68 and `FindRepoSubdir(...)` at lines 70-83, both walking upward from `AppDomain.CurrentDomain.BaseDirectory` and throwing `"Could not locate ... in any ancestor of the test binary."` `pinder-core/tests/Pinder.Core.TestCommon/TestRepoLocator.cs:13` already exposes the same ancestor-walk `FindRepoSubdir(...)` behavior, backed by `Pinder.Core.Data.DataFileLocator.FindRepoRoot(...)`.
**Impact**: Test data lookup behavior is split between a private copy and the shared test locator. Build-layout, repo-root marker, or data directory changes can fix shared tests while leaving this regression suite on stale path logic.
**Urgency**: U3 — topic default.
**Fixer-Agent Action Plan**: Replace `FindRepoSubdir(...)` calls with `TestRepoLocator.FindRepoSubdir(...)`. Add a shared `ReadDataFile` or `FindDataFile` helper to `Pinder.Core.TestCommon` if needed, then remove `LoadJson(...)` and run the Issue840 and data-locator tests.

### Finding 5: TurnResult test fixtures are copied across frontend component and e2e tests
**File**: `pinder-web/frontend/e2e/message-timeline.spec.ts:45`
**Issue**: `message-timeline.spec.ts` defines a full `baseResult(): TurnResult` object at lines 45-126, including repeated `roll`, `state_after`, `steering`, `horniness_check`, and `shadow_check` defaults. Similar local builders exist in `frontend/e2e/steering-block.spec.ts:23`, `frontend/e2e/d20-visual.spec.ts:48`, `frontend/e2e/nat-die-explainer.spec.ts:45`, `frontend/e2e/roll-formula.spec.ts:46`, and component tests such as `frontend/src/components/TurnResultDisplay.1034.test.tsx:38`, `TurnResultDisplay.974.test.tsx:37`, `TurnResultDisplay.975.test.tsx:37`, `TurnResultDisplay.976.test.tsx:40`, and `TurnResultDisplay.unifiedStack.test.tsx:62`. A shared builder already exists at `frontend/src/test-fixtures/turnResultBuilder.ts:123` with `makeTurnResult(...)`, `makeRollResult(...)`, `makeGameStateSnapshot(...)`, `makeSteeringRoll(...)`, `makeHorninessCheck(...)`, and `makeShadowCheck(...)`.
**Impact**: The frontend `TurnResult` contract has a broad payload surface. Adding or renaming a required field can leave some tests using the maintained builder while many copied fixtures keep stale defaults or cast through incomplete objects.
**Urgency**: U3 — topic default.
**Fixer-Agent Action Plan**: Migrate the duplicated local builders to `makeTurnResult(...)` and the narrower helper builders, exposing an e2e-friendly import path if needed. Keep only fixture-specific overrides in each test file and run the TurnResultDisplay vitest suite plus the affected Playwright specs.

### Finding 6: FastAPI admin tests repeat auth override and cleanup fixtures
**File**: `pinder-web/src/pinder-backend/tests/test_admin_content_write.py:62`
**Issue**: `test_admin_content_write.py` defines `_override_user(...)` at lines 62-67 and `_clear_overrides_between_tests()` at lines 70-73, directly mutating `app.dependency_overrides[require_auth]` with `auth_test_user(...)`. The same pair appears in many admin suites, including `test_admin_content_items.py:58`, `test_admin_content_traps.py:55`, `test_admin_content_game_definition.py:84`, `test_admin_content_prompts.py:46`, `test_admin_prompts_catalog.py:26`, `test_admin_prompts_update.py:32`, `test_admin_speculate.py:20`, `test_admin_token_usage.py:20`, and `test_prompt_staging_gate.py:48`; a repository-wide check found 32 `_override_user`/`_clear_overrides_between_tests` definitions. `_test_helpers.py:44` centralizes `auth_test_user(...)`, but not the override/cleanup pattern that every suite repeats.
**Impact**: Changes to the admin claim default, auth dependency target, or dependency cleanup semantics must be patched across many test files. A stale copy can leak overrides between tests or validate the wrong admin/non-admin contract.
**Urgency**: U3 — topic default.
**Fixer-Agent Action Plan**: Move the override helper and autouse cleanup fixture into `src/pinder-backend/tests/conftest.py` or `_test_helpers.py`, returning a callable fixture such as `override_user(sub, is_admin=None)`. Replace local definitions in admin suites and run the FastAPI admin/auth test subset.

### Finding 7: Prompt editors duplicate custom save/warning state outside the shared admin editor hook
**File**: `pinder-web/frontend/src/pages/AdminPage.promptsEditor.tsx:65`
**Issue**: `PromptsEditorInner` hand-rolls `saved`, `draft`, `saveState`, `saveWarning`, `initialRef`, `dirty`, `onSave`, a 4-second saved toast timeout, an 8-second warning timeout, and inline save/error/warning button UI at lines 65-190. `DramaticArcPromptEditor` repeats the same pattern at `pinder-web/frontend/src/pages/DramaticArcPromptEditor.tsx:52`, including `saved`/`draft`/`saveState`/`saveWarning`, dirty calculation, reload-warning handling, saved-toast timeout, and save button rendering at lines 52-164. Other admin editors already use `useAdminDraftEditor(...)` from `frontend/src/pages/AdminEditorState.ts:14` and `AdminEditorSaveControls` from `frontend/src/pages/AdminEditorShell.tsx:25`.
**Impact**: Prompt save behavior is split from the shared admin editor surface. Reload-warning timing, success/error formatting, disabled-state logic, and dirty-state reset behavior can drift between prompt editors and the rest of the admin content editors.
**Urgency**: U3 — topic default.
**Fixer-Agent Action Plan**: Extend `useAdminDraftEditor` or `useSavePipeline` with a response hook for warning reload statuses and expose a warning slot in `AdminEditorSaveControls`. Migrate `PromptsEditorInner` and `DramaticArcPromptEditor` to the shared hook/control while preserving prompt-specific field rendering, then run the admin prompt editor tests.
