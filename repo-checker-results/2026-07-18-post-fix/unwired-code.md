> Scope: full pinder-core and pinder-web repositories at the finalized post-fix main commits.

### Finding 1: Shadow-aware datee timing calculator is bypassed
**File**: `pinder-core/src/Pinder.Core/Conversation/DateeTimingCalculator.cs:12`
**Issue**: `DateeTimingCalculator.ComputeDelayMinutes(...)` is a production class/method that models interest, datee shadow values, dry spells, and previous-delay fixation, but exact symbol search found no production caller of `DateeTimingCalculator` or `ComputeDelayMinutes`. The live turn path still computes datee delay with `datee.Timing.ComputeDelay(state.Interest.Current, rollStage.ResolveDice)` in `pinder-core/src/Pinder.Core/Conversation/DateeResponseStage.cs:50`; that legacy method only uses interest and variance from `pinder-core/src/Pinder.Core/Conversation/TimingProfile.cs:41`.
**Impact**: The tested shadow-aware datee timing rules never affect runtime response delay. Datee Overthinking, Denial, Madness, and Fixation can change gameplay state but cannot change the `DateeContext.ResponseDelayMinutes` fed to the LLM.
**Urgency**: U2 - escalated from U3 because this is a tested gameplay mechanic bypassed on every production Speak turn.
**Fixer-Agent Action Plan**: Wire `DateeResponseStage` through `DateeTimingCalculator.ComputeDelayMinutes(...)`, building the datee shadow dictionary from `state.DateeShadows` and persisting previous response delay if Fixation should reuse it. Update dice-budget tests if the richer calculator consumes extra rolls, and add an integration test proving a high datee shadow changes `DateeContext.ResponseDelayMinutes`.

### Finding 2: Player response delay penalties are modeled but hardwired off
**File**: `pinder-core/src/Pinder.Core/Conversation/PlayerResponseDelayEvaluator.cs:10`
**Issue**: `PlayerResponseDelayEvaluator.Evaluate(...)` returns a `DelayPenalty`, and `TurnResult` has a `delay_penalty` interest-breakdown source at `pinder-core/src/Pinder.Core/Conversation/TurnResult.cs:250`, but exact symbol search found no production caller of `PlayerResponseDelayEvaluator`. The live resolver constructs every `TurnResult` with `delayPenalty: 0` in `pinder-core/src/Pinder.Core/Conversation/TurnOrchestrator.Resolve.cs:161`. The downstream API/frontend still preserve this source in `pinder-web/contracts/game-api.yaml:1909` and `pinder-web/frontend/src/data/interestMath.ts:115`.
**Impact**: Slow player replies never reduce interest and never trigger the modeled delay test prompt. The API and UI carry a `delay_penalty` concept that is effectively always zero in live turns, which can make telemetry, player feedback, and balancing work misleading.
**Urgency**: U2 - escalated from U3 because a modeled gameplay penalty is hardwired off in the production turn resolver.
**Fixer-Agent Action Plan**: Persist or derive the elapsed time between the datee response and the next player message, call `PlayerResponseDelayEvaluator.Evaluate(...)` before the `TurnResult` is created, pass the actual `DelayPenalty.InterestDelta`, and thread `DelayPenalty.TestPrompt` into datee context when `TriggerTest` is true. Add an integration test for a long reply producing a nonzero `delay_penalty`.

### Finding 3: Background-story generator and prompt catalog entry have no execution path
**File**: `pinder-core/src/Pinder.SessionSetup/LlmBackgroundGenerator.cs:29`
**Issue**: `LlmBackgroundGenerator` implements `IBackgroundGenerator` and requires the `"background"` prompt catalog key, but exact symbol search found no production construction or call of `LlmBackgroundGenerator` or `IBackgroundGenerator`. The shipped `pinder-core/data/prompts/background.yaml:5` says the feature is called once per character creation, while GameApi exposes only `Backstory`, `Stake`, `Diagnosis`, and `Bio` in `pinder-web/src/Pinder.GameApi/Services/CharacterSynthesisStage.cs:3`; `pinder-web/src/Pinder.GameApi/Controllers/CharactersController.cs:802` aliases incoming `"background"` to `CharacterSynthesisStage.Backstory`.
**Impact**: Admin/API flows can surface `background_story`, but the configured 3-5 sentence background synthesis prompt is never run. A request to regenerate "background" actually regenerates backstory categories, so downstream UI may show derived tragic realities instead of the intended generated background prose.
**Urgency**: U2 - escalated from U3 because a configured character-generation feature is exposed in API/UI language but cannot execute.
**Fixer-Agent Action Plan**: If separate background prose is still intended, add a `Background` synthesis stage, wire `LlmBackgroundGenerator` through `CharacterSynthesisService`, controller parsing, and the frontend stage union, and persist a real `background_story` field through loader/writer and DTO tests. If the feature is obsolete, delete `IBackgroundGenerator`, `LlmBackgroundGenerator`, `background.yaml`, and the misleading `"background"` alias/DTO comments.

### Finding 4: GameApi session query helper is never imported or called
**File**: `pinder-web/src/Pinder.GameApi/Data/GameSessionQueryHelper.cs:18`
**Issue**: `GameSessionQueryHelper` defines production extension methods for loading session state, frozen sheets, turn records, user/admin session lists, share rows, conversation history, and token usage, but exact symbol search found no reference to `GameSessionQueryHelper` and no `using static Pinder.GameApi.Data.GameSessionQueryHelper` anywhere under `pinder-web/src/Pinder.GameApi`. Because these are extension methods on `IDbContextFactory<PinderDbContext>`, they are not discovered by reflection; without an import/call site the entire facade is dead.
**Impact**: The repository carries a parallel read-only data access layer that cannot execute in production. Fixes made in this helper would not affect GameApi behavior, and future maintainers may choose the wrong query path when investigating session listing, sharing, debug, or token usage issues.
**Urgency**: U3 - topic default; this is stale production surface with maintainability risk, but the active repository paths appear to keep current behavior working.
**Fixer-Agent Action Plan**: Either delete `GameSessionQueryHelper` and any tests that only protect it, or refactor the active `GameSessionRepository`/controller read paths to call these extension methods as the single query implementation. If wiring it, add integration coverage for user listing, admin listing, share lookup, history loading, and token usage through the public repository/API entry points.

### Finding 5: C# failure-tier display helper is not used by any production projection
**File**: `pinder-core/src/Pinder.Core/Rolls/FailureTierDisplay.cs:6`
**Issue**: `FailureTierDisplay.DisplayNameKey(...)` maps `(FailureTier, RollCheckKind)` to `display_names.failure_tier.*` keys, but exact symbol search found no production caller; only tests/docs reference it. The wired web UI instead resolves tier copy in `pinder-web/frontend/src/data/failureTierDisplayLabel.ts` and related TurnEvent/EventBox renderers, so the C# helper is not the source of truth for any live DTO or UI path.
**Impact**: Backend code appears to own failure-tier label policy, but updates there will not change player-visible text. Future work can patch this helper and miss the actually wired frontend mapping.
**Urgency**: U3 - topic default; the live frontend label path is wired, but this production helper is stale API surface.
**Fixer-Agent Action Plan**: Either remove `FailureTierDisplay` and its tests if frontend-side localization is authoritative, or wire it into GameApi DTO/projection output and add a contract test that C# display keys and frontend display labels remain aligned.
