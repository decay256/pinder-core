<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>NarrativeHarness</AssemblyName>
    <RootNamespace>Pinder.Tools.NarrativeHarness</RootNamespace>
  </PropertyGroup>

  <!--
    ZERO rule code paths (issue #843 hard requirement):
    This tool deliberately does NOT reference Pinder.Rules. It drives only
    SessionSystemPromptBuilder -> Pi provider transport. Do not add a
    Pinder.Rules ProjectReference here.
  -->
  <ItemGroup>
    <PackageReference Include="Pi.AI.Transports.HttpClient" Version="0.1.0-alpha.2" />
    <ProjectReference Include="..\..\src\Pinder.NarrativeHarness\Pinder.NarrativeHarness.csproj" />
  </ItemGroup>

</Project>
