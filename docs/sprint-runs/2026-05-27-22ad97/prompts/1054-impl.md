You are a backend engineer subagent in the Pinder dev swarm.

## Workspace setup
We must use workspace isolation for this task:
```bash
cd /root/projects/pinder-core
git fetch origin
git worktree add /tmp/work-1054 origin/main
cd /tmp/work-1054
git submodule update --init
git checkout -b fix/1054-restore-anthropic-caching
```

## Role Specification
Please read the backend engineer role spec at `/root/projects/eigentakt/agents/backend-engineer.md`.

## Lessons and Guidelines
Refer to lessons in the project's `LESSONS_LEARNED.md`, especially:
- `WORKSPACE-ISOLATION`
- `SUBMODULE-SYNC-AFTER-REBASE`
- `AGENT-LOG-EVERYTHING`
- `BUILD-PIPELINE-DISCIPLINE`
Full canonical lesson descriptions can be found in `/root/projects/eigentakt/references/canonical-lessons.md`.

## Project Rules
Also read `AGENTS.md` in the project root for project-specific schema discipline rules.

## Tickets

### #1054 — [bug] Restore Anthropic Prompt-Caching Invariant
The newly consolidated `PinderLlmAdapter` + `AnthropicTransport` pipeline has introduced a prompt-caching regression.

Legacy `AnthropicLlmAdapter` correctly built and serialized `cache_control: {"type": "ephemeral"}` blocks on system prompts and context inputs via `CacheBlockBuilder`. However, the new consolidated adapter pipeline omits these block insertions entirely.

As a result, Claude prompt cache hit rates have dropped to zero, violating our core domain invariants for LLM cost-monitoring and causing significant, ongoing waste of API tokens during test runs and playtests.

**Acceptance criteria:**
- Restore prompt caching block insertions (`cache_control: {"type": "ephemeral"}`) on the system prompts and historical context inputs for all Anthropic model calls.
- Verify that cache blocks are built and serialized properly in `AnthropicTransport.cs` system blocks.
- Run fast tests to ensure no regressions are introduced on the caching serialization path.
- Confirm caching-related test assertions are fully restored and passing.

#### Verification Instructions:
- Run `dotnet test` specifically targeting Anthropic tests to ensure that the caching tests now pass cleanly.
- Capture the test results to `/tmp/test-1054.txt` and report the test summary (pass/fail count).
- Build the project using `dotnet build` to verify there are no compilation errors or warnings.

#### Workflow Rules:
- Commit your work to the branch `fix/1054-restore-anthropic-caching`.
- Push the branch and open a pull request using:
  `gh pr create --repo decay256/pinder-core --base main --head fix/1054-restore-anthropic-caching --fill`
  Include `Closes #1054` in the PR body.
- Append log entries to `agent.log` using `bash /root/projects/eigentakt/scripts/log.sh` for starting and completing.
- Note: All work must be conducted in the `/tmp/work-1054` worktree.

Do not merge or push directly to main. Only open the PR.
